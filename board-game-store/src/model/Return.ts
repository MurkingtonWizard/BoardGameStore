export interface IReturn {
    id: number,
    transaction_id: number,
    board_game_id: number,
    date_time: string,
    quantity: number
}
//Return(return_id, fk_transaction_id, fk_board_game_id, datetime, quantity)
